# no filters or mutators should be placed here, we preserve as much as possible by this stage
output {
  file {
<% if @file['codec'] -%>
    codec => <%= @file['codec'] %> {
    }
<% end -%>
<% if @file['path'] -%>
    path => "<%= @file['path'] %>"
<% end -%>
  }
}
